#include <bits/stdc++.h>
using namespace std;

void insert_vec (int rows, int cols, vector<vector<int>> vect(rows, vector<int>(cols,0))) {

    for (int i = 0 ; i < rows ; i++) {
        vector<int> cols_ele;

        for (int j = 0 ; j < cols ; j++)
            
    }
}
